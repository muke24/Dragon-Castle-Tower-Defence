using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetPositions : MonoBehaviour
{
    public Vector2 subtitleMainMenu;
    public Vector2 subtitleSettings;


    // Start is called before the first frame update
    void Start()
    {
        subtitleMainMenu.x = 4.924194f;
        subtitleMainMenu.y = 0.2788696f;

        subtitleSettings.x = 4.924194f;
        subtitleSettings.y = 0.2788696f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
